name: Deploy React demo to GitHub Pages

on:
  workflow_run:
    workflows: ['Unit Test / Linting']
    types: [completed]
    # The public site tracks the monorepo integration branch while main and
    # feat/react retain their pre-migration histories.
    branches: ['feat/monorepo']

# Sets the GITHUB_TOKEN permissions to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'deploy'
  cancel-in-progress: true

jobs:
  deploy:
    if: github.event.workflow_run.conclusion == 'success'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Set up pnpm
        uses: pnpm/action-setup@v4.4.0

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Build renderer demos
        run: pnpm build:demo --base "/${{ github.event.repository.name }}/"

      - name: Build Vue research demo
        run: pnpm build:demo:vue --base "/${{ github.event.repository.name }}/vue/"

      - name: Add Vue demo to Pages artifact
        run: cp -R apps/playground-vue/dist apps/docs/dist/vue

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: apps/docs/dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
