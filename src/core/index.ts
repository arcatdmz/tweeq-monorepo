// Temporary Stage V1/V2 alias (plan §5): keeps `src/react` and demo imports
// of '../core' working while implementations live in workspace packages.
// Deleted when src/react moves to packages/react.
export * from '@tweeq/core'
export * from '@tweeq/dom'
