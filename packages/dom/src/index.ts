// @tweeq/dom — framework-neutral browser integration (plan §3.1).
// DOM access happens inside factories/methods only, never at import time.
// Populated in Phase 2 by relocating the browser-facing parts of `src/core`.
export {}
