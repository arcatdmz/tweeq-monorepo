// Stage V2: the single validator implementation lives in @tweeq/core; this
// module keeps `import * as V from '../validator'` and the public
// `export * as validator` surface working unchanged.
export * from '@tweeq/core/validator'
