// Temporary Stage V1 alias: the shared sheet is owned by @tweeq/styles.
// Stylus `@import` cannot resolve package exports, so this forwards to the
// sibling workspace package by path. Tracked in the parity-matrix deletion
// checklist.
@import '../../styles/src/common.styl'
