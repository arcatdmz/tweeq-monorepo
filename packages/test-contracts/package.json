{
	"name": "@tweeq/test-contracts",
	"version": "0.0.0",
	"private": true,
	"description": "Renderer-neutral conformance suites for Tweeq (test-only, never published)",
	"type": "module",
	"types": "./src/index.ts",
	"main": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"build": "tsc -p tsconfig.json --noEmit"
	},
	"devDependencies": {
		"typescript": "catalog:",
		"vitest": "catalog:"
	}
}
