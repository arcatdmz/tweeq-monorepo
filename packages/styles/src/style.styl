// @tweeq/styles — the shared visual implementation (plan §3.1).
// Owns tokens (`--tq-*`), reset/viewport styles, and stable component parts
// (`.tq-*`, `[data-tq-part]`) scoped under `.TqViewport`.
// Populated during Phase 3; renderer-local styles migrate here only after
// behavioral parity (Stage V3).
.TqViewport
	// intentionally empty until Phase 3
	--tq-styles-placeholder 0
