{
	"name": "@tweeq/styles",
	"version": "0.0.0",
	"private": true,
	"description": "Design tokens, reset, and shared component style parts for Tweeq",
	"type": "module",
	"exports": {
		"./style.css": "./dist/style.css",
		"./common.styl": "./src/common.styl",
		"./shared.styl": "./src/shared.styl"
	},
	"files": [
		"dist",
		"src"
	],
	"scripts": {
		"build": "rm -rf dist && mkdir -p dist && stylus --compress --out dist src/style.styl && node ../../scripts/append-monaco-styles.mjs"
	},
	"devDependencies": {
		"monaco-editor": "^0.54.0",
		"stylus": "catalog:"
	}
}
