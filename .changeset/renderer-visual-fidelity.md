---
'@tweeq/react': patch
'@tweeq/vue': patch
---

Preserve nested component style parts and caller sizing in React, prevent a
Dropdown from changing value merely because its popup opened under a stationary
pointer, and align Vue Dropdown chrome and renderer text-node geometry. Expose
the five implemented Vue primitives that were missing from its public barrel,
preserve React BindIcon's caller part, and keep the exhaustive renderer
galleries on one complete visual and ordering contract.
